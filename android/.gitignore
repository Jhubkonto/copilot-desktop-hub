# Android build artifacts
*.iml
.gradle/
/local.properties
/build/
app/build/

# IDE
/.idea/caches/
/.idea/libraries/
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store

# NDK
/captures/
.externalNativeBuild/
.cxx/

# Keystore (never commit signing keys)
*.keystore
*.jks

# Firebase — a placeholder google-services.json is committed so the build works without Firebase setup.
# To enable FCM push notifications: replace app/google-services.json with the real file from
# your Firebase console (Project settings → General → Your apps → google-services.json).
